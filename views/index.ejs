<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style/main.css"></link>
</head>
<body>
    <div class="container">
        <h1>My Blog</h1>
        <a class="newpostbtn" href="/new">New</a>
        <% if (locals.posts) {%>
            <ul class="postList">
                <% posts.forEach(post => { %>
                    <li><strong><h2><%= post.title %></h2></strong></li>
                    <li><small><%= post.date %></small></li>
                    <li><%= post.content %></li>
                    <li><small><%= post.author %></small></li>
                    <a class="edit" href="/edit/<%=post.id%>">Edit</a>
                    <a class="delete" href="/delete/<%=post.id%>">Delete</a>
                    <hr>
                <% })%>
            </ul>
        <% }%>    
    </div>
</body>
</html>